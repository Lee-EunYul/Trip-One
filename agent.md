# Agent Guide: Trip One Vibe Coding

## 1) 문서 목적
이 문서는 Trip One을 바이브 코딩 방식으로 빠르게 구현할 때, AI 코딩 에이전트가 일관된 기준으로 설계/개발/검증하도록 안내하는 실행 지침이다.

핵심 원칙:
- 작은 단위로 빠르게 만들고 바로 검증한다.
- PRD 요구사항을 벗어나지 않는다.
- "작동하는 것" + "설명 가능한 것"을 우선한다.

---

## 2) 제품 한 줄 정의
Trip One은 첫 해외여행객을 위해, 여행 프로필 + 항공권 OCR + AI 추천을 결합하여 현실적인 일정/쇼핑/회화/문화 가이드를 제공하는 통합 여행 플랫폼이다.

---

## 3) 에이전트 역할 정의
에이전트는 아래 역할을 동시에 수행한다.

1. Product Engineer
- PRD 기능을 실제 사용자 흐름 기준으로 구현한다.

2. AI Feature Integrator
- OCR, 추천, 번역, 환율 등 외부 연동 기능을 안정적으로 연결한다.

3. QA Assistant
- 기능 개발 후 즉시 테스트 시나리오를 실행하고 결과를 남긴다.

4. Documentation Keeper
- API, 데이터 모델, 결정사항을 문서화해 다음 작업자가 바로 이어받을 수 있게 한다.

---

## 4) 절대 준수 규칙 (Guardrails)

### 4.1 범위 규칙
- v1 MVP 범위 밖 기능은 구현하지 않는다.
- PRD의 오픈 이슈는 "결정 필요"로 표시하고 임의 확정하지 않는다.

### 4.2 품질 규칙
- 새 기능마다 최소 1개 이상의 검증 시나리오를 함께 만든다.
- 에러/예외 흐름(실패 케이스)을 반드시 구현한다.
- 더미 데이터라도 사용자 화면이 끝까지 흐르도록 연결한다.

### 4.3 안전 규칙
- API 키/비밀값을 코드에 하드코딩하지 않는다.
- 개인정보/항공권 이미지 처리는 최소 수집, 최소 보관 원칙을 따른다.

### 4.4 구현 우선순위 규칙
- "사용자 플로우가 닫히는 기능"을 먼저 만든다.
- 완벽한 최적화보다 작동/검증/개선 루프를 우선한다.

---

## 5) 개발 우선순위 (MVP 순서)
에이전트는 아래 순서를 기본으로 구현한다.

1. 인증/기본 레이아웃
- 로그인 가능한 최소 진입점

2. 여행 프로필 입력
- 국가/도시/기간/예산/스타일/동행/첫 여행 여부 입력 및 저장

3. 항공권 OCR + 검수
- 업로드 -> OCR 결과 표시 -> 사용자 수정 -> 확정 저장

4. 일정 생성 엔진(규칙 기반 1차)
- 첫날/마지막날 시간 보정 규칙 반영

5. AI 여행 플래너 UI
- 일자별 카드 + 예산 요약 + 동선/숙소 표시

6. 스마트 쇼핑 + 환율
- 추천 상품/가격 환산/쇼핑리스트 합계

7. Travel Helper
- 기본 회화 10개 + 번역 + 문화 가이드 + 즐겨찾기

8. 로깅/예외처리/기본 성능 점검

---

## 6) 기능별 구현 규칙

### 6.1 여행 프로필
완료 조건:
- 필수값 검증
- 저장 성공/실패 메시지
- 저장 후 다음 단계(OCR)로 이동 가능

검증 질문:
- 입력 누락 시 사용자가 무엇을 해야 하는지 명확한가?

### 6.2 항공권 OCR
완료 조건:
- 업로드 가능 포맷: JPG/PNG/PDF
- OCR 결과 + 신뢰도 표시
- 수동 수정 후 확정 저장

실패 처리:
- OCR 실패 시 수동 입력 폼 자동 전환

### 6.3 일정 생성
완료 조건:
- 첫날/마지막날 항공편 시간 반영
- 일정 항목별 추천 이유 포함
- 예산 초과 시 경고 + 대안 옵션

기본 규칙(필수):
- 늦은 도착: 저강도 일정
- 이른 출국: 공항 이동 우선

### 6.4 쇼핑 + 환율
완료 조건:
- 추천 상품 리스트
- KRW 환산
- 카테고리별 예산 분배
- 쇼핑리스트 합계 실시간 갱신

실패 처리:
- 환율 API 실패 시 캐시 환율 + 시각 표기

### 6.5 Travel Helper
완료 조건:
- 기본 회화 10개
- 번역 입력/결과 + TTS 트리거
- 문화 가이드 표시
- 즐겨찾기 저장/해제

---

## 7) 기술 의사결정 원칙

### 7.1 아키텍처
- 프론트/백엔드 분리 가능 구조를 유지한다.
- 외부 API 연동부는 어댑터 계층으로 분리한다.
- 도메인 모델(TripProfile, FlightInfo, Itinerary...)을 코드에서 명시적으로 유지한다.

### 7.2 AI/추천 전략
- v1은 규칙 기반 + LLM 보조 설명 방식으로 시작한다.
- 사용자 피드백 데이터 축적 후 개인화 모델로 확장한다.

### 7.3 데이터 처리
- 시간대(Timezone) 처리를 명시한다.
- 금액 계산은 통화 단위/반올림 규칙을 통일한다.

---

## 8) 코드 스타일 가이드
- 함수는 "한 가지 책임"만 갖게 만든다.
- 하드코딩 문자열은 상수/설정으로 분리한다.
- 복잡한 로직에는 짧은 주석으로 의도를 기록한다.
- 불필요한 추상화보다 읽기 쉬운 구조를 우선한다.

권장 패턴:
- 입력 검증 -> 도메인 처리 -> 응답 생성
- 성공 경로 + 실패 경로를 함께 작성

---

## 9) 테스트 가이드 (최소 기준)
각 기능마다 아래 3종 테스트를 우선 작성한다.

1. Happy Path
- 정상 입력 시 기대 결과가 나오는가

2. Validation Fail
- 필수값 누락/형식 오류 시 적절히 막히는가

3. External Failure
- OCR/환율/번역 API 장애 시 대체 동작하는가

핵심 경계값 테스트:
- 도착 시간: 오전/오후/심야
- 출국 시간: 이른 시간/늦은 시간
- 예산: 매우 낮음/평균/높음

---

## 10) 작업 단위 템플릿 (에이전트 실행 포맷)
에이전트는 작업할 때 항상 아래 형식으로 진행한다.

1. 목표
- 이번 작업으로 사용자에게 제공할 가치 1줄

2. 변경 파일
- 어떤 파일을 수정/생성하는지 목록

3. 구현 내용
- 핵심 로직과 UI/API 변경점

4. 검증 결과
- 실행한 테스트와 결과

5. 다음 작업
- 바로 이어서 할 1개 기능

---

## 11) 커밋/기록 규칙
커밋 메시지 예시:
- feat(profile): 여행 프로필 입력/검증/저장 구현
- feat(ocr): 항공권 OCR 결과 검수 플로우 추가
- feat(planner): 첫날/마지막날 시간 보정 규칙 적용
- fix(exchange): 환율 API 실패 시 캐시 폴백 처리

작업 로그에는 아래를 남긴다.
- 결정 사항
- 미결 사항
- 리스크

---

## 12) Definition of Done (MVP)
아래 항목이 모두 충족되면 MVP 완료로 본다.

1. 사용자가 프로필 입력부터 일정 확인까지 끊김 없이 진행 가능
2. 항공권 OCR 실패 시에도 수동 입력으로 동일 목적 달성 가능
3. 첫날/마지막날 일정이 항공편 시간 규칙을 위반하지 않음
4. 쇼핑 예산/환율 계산이 화면에서 즉시 반영됨
5. 회화/번역/문화 가이드가 최소 기능으로 동작함
6. 주요 실패 케이스에 대한 예외 메시지/폴백이 존재함

---

## 13) 바이브 코딩 운영 팁
- 막히면 큰 리팩터링 대신 "동작하는 최소 대안"으로 우회한다.
- UI가 비어 있지 않게 스켈레톤/더미 상태를 먼저 만든다.
- API 연결 전에도 프론트 플로우를 Mock으로 먼저 끝낸다.
- 기능 1개 완료마다 즉시 실행/검증/기록한다.

---

## 14) 이 문서의 사용 방법
- 새 작업 시작 전: 이 문서 5, 6, 9, 12 섹션을 먼저 확인한다.
- 기능 구현 중: 10번 템플릿으로 작업 상태를 기록한다.
- 배포 전: 12번 DoD 체크리스트를 기준으로 최종 점검한다.

Trip One의 목표는 "정보를 많이 보여주는 서비스"가 아니라, 
초보 여행자가 실제로 실행 가능한 계획을 빠르게 만드는 서비스임을 항상 우선한다.
